<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="new-course-field">
    <% f.label "Create a New Course", :id => "new-course-label"%>
    <%= f.text_field :title, :id => "new-course-form", :placeholder => "Start by Creating a New Course"%>
  </div>
  <div class="field">
    <%= f.label :unit_list, "Add Course Units (separated by commas)", :id= %>
    <%= f.text_field :unit_list %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
